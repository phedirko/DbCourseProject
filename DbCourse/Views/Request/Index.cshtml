@model List<List<string>>


<br />
<br />
<h1>Type your sql command</h1>
<div class="ui grid">
    <div class="sixteen wide column">
        <div class="ui massive icon input fluid">
            <input type="text" placeholder="Command..." id="query">
            <i class="database icon"></i>
        </div>
        <br />
        <button class="ui basic button fluid" id="createQuery" onclick="husl">create query</button>
    </div>
</div>


@*@if (Model.Count != 0)
{
    <div class="ui grid">
        <div class="sixteen wide column">
            <table class="ui single line table fluid">
                <thead>
                    <tr>
                        @for (int i = 0; i < Model[0].Count; i++)
                        {
                            <td>@Model[0][i]</td>
                        }
                    </tr>
                </thead>
                <tbody>
                    @for (int row = 1; row < Model.Count; row++)
                    {
                        <tr>
                            @for (int column = 0; column < Model[row].Count; column++)
                            {
                                <td>@Model[row][column]</td>
                            }
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
}*@

<script>
    function husl() {
        $.ajax({
            type: "POST",
            data: { q: $("#query").val() },
            url: "../Request/Index/",
            success: function(result) {
                $("#sqlResult").html(result);
            }
        });
    }   

</script>